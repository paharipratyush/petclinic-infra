image:
  # Filled automatically by scripts/generate-config.sh after terraform apply
  repository: PLACEHOLDER_ECR_REGISTRY/petclinic-dev/genai-service
  tag: v1.0.0
service:
  port: 8084
# genai-service stays at 1 replica in prod — lower priority, cost saving
replicaCount: 1
env:
  - name: SPRING_PROFILES_ACTIVE
    # production is default for genai-service per application.yml
    value: "docker,production"
  - name: CONFIG_SERVER_URL
    value: "http://config-server:8888"
  - name: OPENAI_API_KEY
    valueFrom:
      secretKeyRef:
        name: openai-api-key
        key: OPENAI_API_KEY
  - name: MANAGEMENT_ZIPKIN_TRACING_ENDPOINT
    value: "http://zipkin.tracing:9411/api/v2/spans"
  - name: MANAGEMENT_TRACING_SAMPLING_PROBABILITY
    value: "1.0"
initContainers:
  - name: wait-for-config-server
    image: busybox:1.36
    command:
      - sh
      - -c
      - 'until wget -qO- http://config-server:8888/actuator/health; do echo "waiting for config-server..."; sleep 5; done'
  - name: wait-for-discovery-server
    image: busybox:1.36
    command:
      - sh
      - -c
      - 'until wget -qO- http://discovery-server:8761/actuator/health; do echo "waiting for discovery-server..."; sleep 5; done'
